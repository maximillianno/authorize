{% extends 'base.html.twig' %}

{% block title %}Magazine{% endblock %}

{% block body %}
    <h1>Magazine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ magazine.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ magazine.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ magazine.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ magazine.date ? magazine.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ magazine.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('magazine_index') }}">back to list</a>

    <a href="{{ path('magazine_edit', {'id': magazine.id}) }}">edit</a>

    {{ include('magazine/_delete_form.html.twig') }}
    {{ render(controller('App\\Controller\\AuthorController::listHisAuthors', {'list': list})) }}
{% endblock %}
